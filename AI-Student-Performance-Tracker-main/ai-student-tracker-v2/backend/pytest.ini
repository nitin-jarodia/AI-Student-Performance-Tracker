[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers

[coverage:run]
source = app
omit =
    app/ml/*
    */__init__.py

[coverage:report]
show_missing = true
skip_covered = false
fail_under = 30
