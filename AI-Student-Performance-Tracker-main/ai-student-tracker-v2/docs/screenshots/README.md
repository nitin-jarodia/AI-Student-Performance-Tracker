# Screenshots

Add PNG captures here for the README demo section:

- `dashboard.png` — teacher dashboard with risk charts
- `students.png` — student list / detail view
- `analytics.png` — ML analytics page

Capture locally after `start.bat` or `docker compose up`, then link them in README:

```markdown
![Dashboard](docs/screenshots/dashboard.png)
```
